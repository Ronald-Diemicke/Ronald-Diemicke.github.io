/**
 * Import all web components so they register with the browser.
 * Include this script once in your layout (e.g. BaseLayout.astro).
 */
import './button/button.js';
import './card/card.js';
import './input/input.js';
